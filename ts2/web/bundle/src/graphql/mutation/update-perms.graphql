mutation UpdatePerms(
    $serverId: ID!
    $readable: [String]!
    $writable: [String]!
) {
    updatePerms(serverId: $serverId, readable: $readable, writable: $writable) {
        server {
            snowflake
            readable
            writable
        }
    }
}
