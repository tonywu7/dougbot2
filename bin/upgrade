#!/usr/bin/zsh
set -eux

git pull
sudo docker-compose build
sudo docker-compose down
sudo docker-compose run bot ./bin/migrate
sudo docker-compose up -d
yes | sudo docker container prune
yes | sudo docker image prune