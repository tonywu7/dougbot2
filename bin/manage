#!/bin/bash
source /application/.venv/bin/activate

if [[ $1 == "web" ]];
then
    exec daphne -b :: -p 8086 ts2.asgi:application;
elif [[ $1 == "bot" ]]
then
    exec python3 manage.py runclient;
else
    exec python3 manage.py "$@";
fi